export enum TaskStatusEnum {
	ToDo = 0,
	InProgress = 1,
	Done = 2,
}

export enum TaskPriorityEnum {
	Low = 0,
	Medium = 1,
	High = 2,
}

export enum RoleEnum {
	Admin = 1,
	User = 2,
	ProjectLeader = 3,
	Developer = 4,
}
